import React from "react";

const JustButtonToRight = ({ children }) => {
  return (
    <div className="flex justify-between align-content-center ">{children}</div>
  );
};

export default JustButtonToRight;
